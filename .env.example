# ========================================
# CONFIGURAÇÕES DO AMBIENTE - TecStock
# ========================================
# Copie este arquivo para .env e ajuste os valores

# Perfil Spring (dev ou prod)
SPRING_PROFILES_ACTIVE=dev

# ========================================
# CONFIGURAÇÕES DO BANCO DE DADOS - DEV (localhost)
# ========================================
DB_HOST_DEV=localhost
DB_PORT_DEV=5432
DB_NAME_DEV=TecStock
DB_USERNAME_DEV=postgres
DB_PASSWORD_DEV=123456

# ========================================
# CONFIGURAÇÕES DO BANCO DE DADOS - PROD (Docker)
# ========================================
DB_HOST_PROD=postgres
DB_PORT_PROD=5432
DB_NAME_PROD=TecStock
DB_USERNAME_PROD=postgres
DB_PASSWORD_PROD=SuaSenhaSeguraAqui123!

# ========================================
# CONFIGURAÇÕES JWT
# ========================================
# IMPORTANTE: Altere esta chave para uma chave segura única em produção!
JWT_SECRET=TecStockSecretKeyForJWTTokenGenerationAndValidation2024!@#$%SecureKey123456789
# Expiração do token em milissegundos (86400000 = 24 horas)
JWT_EXPIRATION=86400000

# ========================================
# CONFIGURAÇÕES OPCIONAIS
# ========================================
# Porta do backend (padrão: 8081)
# SERVER_PORT=8081

# Porta do frontend (padrão: 80)
# FRONTEND_PORT=80
